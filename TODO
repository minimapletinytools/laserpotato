
:: 8/31/2026 MVP actual ytODO

-understanding frame/state semanics
    -frame n is a frame with a completely resolved state
    -frame n* is is a frame with a partially resolved state, when fully resolved will become frame n+1
    -level editor is frame "0*", and game starts at frame 1
    -level editor actually allows showing both frame 0* and frame 1....  frame 1 is just a fake preview. when editing blocks, they must always show frame 0*
    -if resolving to frame 1 pushes block movement onto the state update queue, stop the solver and display an error. the level is invalid and there is no frame 1 preview.

-add support for "Floor" and "level size"
    -floor tiles aren't anything special game logic wise but tehy get special treatement in the editor
    -floor tiles, just regular tiles but maybe the look different (or maybe they just look like regular wall tiles)
    -first make surr you can walk on top of tiles in general, well that should be a property of it, can walk on top property. THis property is actually a perf fact property... mirrors for example have 2 faces that can be walked on!
    -floorplan pop up menu, lets you set a certain Z level filled with floor b0locks (default z=-1) up to some level size (w,h)... 0,0 is still bottom left corner
        -you may need to update the default level to move the floor blocks down to z =-1
    -add a "lock floor" button which takes a z level (auto set by the floorplan popup) and makes all blocks on that level not selectable.

-improve selection
    -create selection mode
    -in both placement modes, alllow shift click to select multiple blocks
    -in z layer placement mode, allow lcick + drag to multi select. clicknig on a block + dragging prioritizes box selection rather than move block
        -in raycast placement mode, no box seleciton, click + dragging on a block moves it
    -create a button that says "combine" that combines all block in a selection into 1 mega block IF all the blocks are moveable, (button is greyed out if they are not)

-make level editor window resizeable if not already

-create glass block (moveable/immoveable) transparent block that laser just passes through.


-improve visual language for testing
    -sharp corner block = immovable
    -rounded corner block = pushable
    -so we need botha sharp and round corner variant of mirror and laser and regular block
    -in general moveable objects are brighter in color as well.
    -for now, we have only the following blocks to support, so this will bethe set we mainly experiment with for our MVP
        -block, moveable/immovable (can be walked on)
        -glass block, moveable/immoveable (can be walked on)
        -mirror, moveable/immoveable (2 surfaces can be walked on)
        -laser, moveable/immovabel (only laser surface can't be walked on)
        -goal block, moveable/immovable (no surface can be walked on)




/resume bd83dde8-d764-40cc-b2a3-85ed22dea10c (where I switched to gemini)


:: 8/24/2026 MVP

MVP goal is get game to a state with minimal mechanics so you can start designing puzzles

-fix state issue in level editor
-think through state/movement logic ordering (see how other games do it)
    -in particular, you will have lasers destroying blocks (that can take 1 turn acutally)
    -also blocks falling to block lasers (maybe also have this take 1 turn)
    -but in general lasers mke things complicated
-add level selector and some way to organize levels
-add ability to save solutions next to levels
-add 3d rotation support to leel editor
-blocks 
    -3d mirrors
    -tubes (for laser splitting)
    -add directional laser goal
    -destructible blocks that can't be moved
    -destructible blocks that can be moved
    -combined movable blocks 
    -glass block, laser passes through but not ppl


:: 8/24/2026 visual language

lets switch gears for a moment and work on styling. we want a layered styling sorta thing. each block has the
  following, base model, color, texture over color, transparency. We will use these 3 properties to build a visual
  language for the blocks. Note that models may not just be hardcoded geometric shapse but we will import stuff with
  textures in the future. Don't worry too harda bout that now, basically in these cases, the texture/color will onyl
  apply to parts of the model, but for hardcode geometric shapes, it will apply to everything.. anyways, build out
  this visual language for block styling. we will layer on top of it. also can you propose anything else we can add
  to this language? I want felxibility for introducing new block types


-combined blocks ideally have their own custom representation... for now, just apply some cute texture on all blocks that are combined
-new block creation factory (not important right now)
    -to facilitate combined block graphics, we need a block creation funciton that generates a custom model for combined blocks.
    -the general factory has a couple of shapes it supports
        -block
        -mirror
        -pyramid
    -each shape has faces that coincide with the block walls, the faces shap may either be at triangle (2 edges) or a square (4 edges)
    -coincididing faces get theri edges combined to create a unified volume
    -if the combined jobect is meovable. round over the actual edges of the combined model


  
:: TODO

DONE-infinite z axis
    -raycast mode 
DONE-level editor block selectionpreview fix pls


-fix toolbar in level edtior

-level structure
    -allow organization by folder
    -each level may have multiple versions and solutions
    -probbaly just 1 folder = 1 level for your own organization sanity...
    -solving a level manually saves a new solution, aut osolve also does this with adifferent postfix
    -changing a level indicates existing solutions ar enull, saving ac hanged level deletes old saved solutions
    -add some save a copy button that makes a new copy of the current level with date prefix or osmething

-try web browser publish and shar ewith anton
-add a little x y z gizmo at the origin so we can easily tell the level orientation
-add grid numbers along the x/y=0 axis 





-pressing escape desselects block pallet, if nothing selecetd in block pallet deselects in edtior


-improve frame 0 state computations
    -we save state -0.5 but then show state 0, but then what happens when we want to change state? do we change from state 0 or -0.5
    -state after returning from test mode needs to reset back to state 0, or state -0.5 and recompute



-do some research into building solve
    -grouping moves?
    -identify certain types of "moves"
    -heuristic, is that actually helpful?
    -identify unwinnable states
    -identify uintuitive moves
        -walk into laser path which will simultaneously disable the laser (miht be solver order dependent...)


:: TODONE

DONE-level editor
    -a menu on the left to choose from list of blocks, zoomable
    -blocks are just their base blocks (block, mirror, laser, wall) and thene there's a property selector that lets you choose if it is stationary or moveable, but note that note all blocks can take both properties, e.g. wallsa re always stationary, in these cases invalid option are greyed out
    -grid shows as before,
    -you can select a block in the grid to, which pops up a modify menu
        -change station/moveable
        -change rotation
        -delete it
    -some expandable menu that shows a list of levels we have created (folderized), and there should be a save, save as, new option 
    -level should have a attempt to solve button (runs in a background thread with 30s timeout)
    -level should have a "test level"
    -if solver found a solution, there should be a test level with solution, solution is keyed to the level hash so that if you make changes it doesnt' try to reuse the same solution.
DONE-do potato solver
DONE-allow block reflection (48 block statse?)
DONE-during level edit mode, laser should reflect
DONE-add rotate level by 90 button in editor
DONE-add ability to place block in 3d in editor (raycast onto existing block mode, and pick Z layer mode)